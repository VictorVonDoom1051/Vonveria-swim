/**
 * Deben coincidir con los tokens de marca por defecto de packages/ui/src/tokens.ts
 * (colors.brand.deep / colors.brand.turquoise). Se duplican aqui a proposito:
 * packages/ui es solo frontend (depende de React), packages/configuration lo
 * consume tambien el backend para poblar BrandingSettings por defecto.
 */
export const DEFAULT_PRIMARY_COLOR = "#0B3C5D";
export const DEFAULT_ACCENT_COLOR = "#1FB6A6";
